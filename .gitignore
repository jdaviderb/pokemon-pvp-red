# Rust build artifacts
/target

# macOS
.DS_Store

# Editor
*.rs.bk
*.swp

# SNES/NES ROM images (copyrighted, large)
*.sfc
*.smc
*.nes
*.z64
*.n64
*.v64

# Prebuilt libretro cores (fetch with cores/fetch.sh)
cores/*.dylib
*.gb
*.gbc
*.ips

# Pokémon Red Color romhack IPS patches (downloaded; copyright)
pokered_color/

# Savestates (game-derived, ROM-specific; capture via /battle/save)
states/
pokemon-memory.md

# sqlite dev database
data.db
data.db-*

# stable session cookie key (dev)
.cookie_key

# local secrets: OAuth client id/secret/redirect, DATABASE_URL, COOKIE_SECRET, DEV, ...
.env
.env.*
!.env.example
cache/
mcp-server/node_modules/
